import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import NavBar from "./components/NavBar";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "พิชานันท์ | Portfolio",
  description:
    "I'm a passionate beginner developer focused on improving my skills and growing in the world of software development. Eager to learn and constantly seeking new challenges, I'm dedicated to expanding my knowledge and taking my coding abilities to the next level.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <NavBar />
        {children}
      </body>
    </html>
  );
}
